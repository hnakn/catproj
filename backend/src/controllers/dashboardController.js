const db=require('../config/database');
exports.summary=async(req,res,next)=>{try{const [summary,latestLogs]=await Promise.all([db.query(`SELECT COUNT(*)::int AS "totalEquipment", COUNT(*) FILTER (WHERE LOWER(status)='free')::int AS "availableEquipment", COUNT(*) FILTER (WHERE LOWER(status)='occupied')::int AS "activeEquipment" FROM equipment`),db.query(`SELECT e.equipmentid AS equipment_id,e.equipmentname AS equipment_name,e.equipmenttype AS equipment_type,e.status,l.latitude,l.longitude,l.runtimehours AS runtime_hours,l.idlehours AS idle_hours,l.fuelconsumption AS fuel_consumption,l.timestamp AS log_timestamp FROM equipment e LEFT JOIN LATERAL (SELECT latitude,longitude,runtimehours,idlehours,fuelconsumption,timestamp FROM equipmentlog WHERE equipmentid=e.equipmentid AND timestamp::date=(SELECT MAX(timestamp::date) FROM equipmentlog) ORDER BY timestamp DESC LIMIT 1) l ON true ORDER BY e.equipmentname`)]);res.json({...summary.rows[0],latestLogs:latestLogs.rows})}catch(e){next(e)}};
