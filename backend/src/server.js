const path=require('path');
require('dotenv').config({ path: path.resolve(__dirname, '../.env') });
const express=require('express'),cors=require('cors'); const equipmentRoutes=require('./routes/equipmentRoutes'),dashboardRoutes=require('./routes/dashboardRoutes'),requestRoutes=require('./routes/requestRoutes'),customerRoutes=require('./routes/customerRoutes'),siteRoutes=require('./routes/siteRoutes'),rfidRoutes=require('./routes/rfidRoutes');
const app=express(); app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:5173'})); app.use(express.json()); app.get('/api/health',(req,res)=>res.json({status:'ok'})); app.use('/api/dashboard',dashboardRoutes); app.use('/api/equipment',equipmentRoutes); app.use('/api/requests',requestRoutes); app.use('/api/customers',customerRoutes); app.use('/api/sites',siteRoutes); app.use('/api',rfidRoutes); app.use((err,req,res,next)=>{console.error(err);res.status(err.status||500).json({message:err.message||'Unexpected server error'})}); app.listen(process.env.PORT||5000,()=>console.log('API listening'));
